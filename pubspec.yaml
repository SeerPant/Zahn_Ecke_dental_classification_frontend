name: zahn_ecke_frontend
description: zahn ecke app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  image_picker: ^1.1.2
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0

  provider: ^6.1.1

  http: ^1.1.0
  dio: ^5.4.0

  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

  flutter_form_builder: ^10.2.0
  form_builder_validators: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
